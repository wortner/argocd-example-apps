apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata: 
  annotations: 
    kubernetes.io/ingress.class: nginx
  name: ingress
  namespace: default
spec: 
  rules: 
    - host: 192.168.10.103
      http: 
        paths: 
          - 
            backend: 
              serviceName: helm-guestbook
              servicePort: http
